services:
  - type: web
    name: royal-ararat-restaurant
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    disk:
      name: restaurant-db-disk
      mountPath: /opt/render/project/src/instance
      sizeGB: 1